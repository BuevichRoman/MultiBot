logs
# Локальные конфиги (шаблоны: config/*.example.json)
config/app.json
config/orchestrator.json
l10n.json
l10n_en.json
l10n_fr.json
l10n_ar.json
node_modules
.wwebjs_auth
.wwebjs_cache
data/wwebjs_test_adapter_neworch
.idea
start.sh
sessions
package-lock.json
dist
.wwebjs_test_auth
sess
taxi-nanny-ui/

# Кеш авторизации администратора: создаётся APIManager при первом входе.
# Держать в репозитории нельзя — внутри рабочие token и u_hash
data/default-api.json

# Настройки локального прогона тестов (пример: tests/.env.example)
tests/.env
